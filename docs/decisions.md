# Decisions and verified facts
Log build-time verifications here (model names, quotas, feed URLs, library versions, Oracle shapes).

| Date | Topic | Finding |
|---|---|---|
| 2026-09-20 | Gradle | Current stable Gradle reported as 9.7.1 (services.gradle.org). CI pins 8.14.3 to match AGP 8.13. |
| 2026-09-20 | AGP | developer.android.com lists AGP 9.4.0 as newest (needs Gradle 9.6+, JDK 17). Not used yet: AGP 9 changes Kotlin/KSP/Hilt integration and could not be test-built here. |
| 2026-09-20 | Android versions | Chosen: AGP 8.13.0, Kotlin 2.2.20, KSP 2.2.20-2.0.2, Hilt 2.57.1, Compose BOM 2025.09.00, Retrofit 3.0.0, OkHttp 5.1.0, Firebase BOM 34.3.0. **Unverified**: Maven was blocked from the build sandbox; the first GitHub Actions run is the check. |
| 2026-09-20 | Package name | `com.naveen.civilscompanion` (`in` is a Kotlin keyword). |
| 2026-09-20 | Fonts | Fraunces, IBM Plex Sans, Noto Sans Telugu (all SIL OFL 1.1) from Fontsource npm packages, converted woff2 -> ttf; Latin/Telugu subsets only. Licences in app assets. |
| 2026-09-20 | Vector search | Not needed until M4. Decide sqlite-vec vs ChromaDB then. |
| 2026-09-20 | Server hosting | Oracle sign-up error. Laptop = default server via Tailscale (free personal plan, HTTPS certs, `tailscale serve`); cloud (Oracle/Hetzner ~EUR 5.49 CX23/GCP e2-micro US-only) is the later move. Free hosts (Render/Koyeb) rejected: sleep + no persistent disk. Tailscale commands not yet run by us; verify with `tailscale serve --help`. |
| 2026-09-20 | LLM models (M2) | Default chain `gemini:gemini-3.5-flash-lite` -> `gemini:gemini-3.8-flash` -> `groq:openai/gpt-oss-20b`, taken from Google/Groq model pages at build time. Names change often: the owner runs `python -m app.llm.check` on the server, and the chain is one line in `.env` (`LLM_CHAIN`), no code change needed. Own daily budgets: Gemini 400, Groq 1000 requests (conservative; real free limits are shown in AI Studio / Groq console). |
| 2026-09-20 | Scheduler | Spec says a separate worker container. Deviation: APScheduler runs inside the API process (one container, one SQLite writer, simpler on a laptop). Jobs are rebuilt from the Settings table at start and whenever briefs settings change. Move to a worker later if the API ever gets heavy. |
| 2026-09-20 | Dedup | Uses cleaned URL + title-word overlap (Jaccard >= 0.6) instead of embeddings. Embeddings/sqlite-vec come in M4 with the syllabus; this keeps the Docker image small now. |
| 2026-09-20 | News feeds | `data/feeds.yaml` lists candidate feeds (PIB, The Hindu, Indian Express, RBI). URLs are UNVERIFIED from the build sandbox: run `python -m app.pipelines.news.verify_feeds` on the laptop server; it writes `docs/feeds-report.md`. Disable dead ones with `enabled: false`. robots.txt is honoured for feeds and articles. Article text is read only to summarise and is never stored: only summary, metadata and link. |
| 2026-09-20 | Blocked sites | WebFetch refuses some news sites (The Hindu, Indian Express). Not worked around; their feeds are fetched by the owner's own server at run time under robots.txt rules. |
| 2026-09-20 | TTS | Piper (`piper-tts` pip package, CLI `piper -m <voice>.onnx -f out.wav`, voices fetched with `python -m piper.download_voices`) then ffmpeg to 64 kbps mono MP3. Default voice `en_GB-alan-medium`; `python -m app.tts.samples` makes samples of six voices so the owner can choose. Piper CLI flags are from its docs; first real run on the server is the check. |
| 2026-09-20 | Push for briefs | FCM data-only message (high priority) so the app builds its own notification with Play / Open buttons. Tablet also catches up via `GET /sync/pull`, so a missed push loses nothing. |
| 2026-09-20 | Android M2 libraries | Room 2.8.0 (KSP), Media3 1.8.0 (exoplayer, session, datasource-okhttp), WorkManager 2.10.3. **Unverified** (no Maven access here); CI is the check. Workers and receivers reach Hilt objects through an `@EntryPoint` (no hilt-work dependency). FTS is not added yet: hidden news stays searchable on the server, on-device search arrives with Notes (M4). |
| 2026-09-20 | Player design | One `PlaybackService` (Media3 `MediaSessionService`) owns ExoPlayer; the screen controls it through a `MediaController`. Lock screen, headphone buttons and Bluetooth come from the media session. Sleep timer runs in the app process, which stays alive while the service plays. Saved files are played first; otherwise audio streams from the server with the login token. |
| 2026-09-20 | Alarms | Local exact alarms at each brief time (fallback to inexact when Android 12+ exact-alarm permission is off) re-registered on boot/app update. They only trigger a sync + notification; the server still prepares the brief 30 minutes earlier. Same notification id per brief, so a push and an alarm never show twice. |
| 2026-09-21 | M3-M12 build mode | The owner was away, so M3 to M12 were built in one unattended pass by 7 feature builders plus reviewers, on top of a shared foundation (generic Room `records` table, generic `/sync/pull` + `/sync/push`, job queue, feature loader). Every assumption is in `docs/agent-notes/*.md`. Nothing on Android has been compiled: the first GitHub Actions run is the real check. |
| 2026-09-21 | Unverified Android libraries | ML Kit text-recognition 16.0.1, CameraX 1.4.2, Glance 1.1.1 (plus Room 2.8.0, Media3 1.8.0, WorkManager 2.10.3 from M2) could not be checked on Maven from the build sandbox. Capture uses the system camera (`TakePicture`), so CameraX is declared but not used. If CI reports a version problem, change the single line in `android/gradle/libs.versions.toml`. |
| 2026-09-21 | Syllabus data | The four starter outlines in `data/syllabus/` (UPSC and APPSC, prelims and mains) are written from general knowledge and marked `verified: false`. The app says so. Replace them with the official syllabus via Syllabus > Import. |
| 2026-09-21 | Sources list | `data/sources.yaml`: every link is `verified: false` (no internet in the build sandbox); the app shows '(link not checked yet)'. Two Andhra Pradesh items have no link. |
| 2026-09-21 | Telugu data | `data/telugu/` (192 items) is general-purpose practice material, `source: general`, not from an official syllabus, and the Telugu text has not been checked by a native reader. The app labels it. Telugu text in server PDFs is left out (IBM Plex has no Telugu, `uharfbuzz` is not installed); the tablet shows everything. |
| 2026-09-21 | FSRS | FSRS-6 implemented twice (Python `app/srs`, Kotlin `srs/`). Both are tested against the shared vectors `data/fsrs_vectors.json` (48 review + 12 strength vectors), generated with the `fsrs` 6.3.2 package. Defaults: 20 new cards per day, snooze moves a card to 06:00 IST, 1 to 3 days ahead. |
| 2026-09-21 | Budget classes | `ocr_page` and the other heavy jobs are deferrable (wait when the day's AI budget is nearly used); `brief_item`, `tutor_answer` and `check` are protected. |
| 2026-09-21 | Videos | YouTube: only titles, channel, ids, links and the owner's notes are stored; nothing is downloaded. Summaries are made from title, channel, topic and notes, never from the video. Search needs `YOUTUBE_API_KEY` (optional, free quota). |
| 2026-09-21 | Focus mode | Do Not Disturb uses priority-only mode through `ACCESS_NOTIFICATION_POLICY`; without the permission a plain timer runs. |
| 2026-09-21 | Weekly report / mock push | `weekly_report` and `mock_ready` are silent data pushes; `CivilsMessagingService` now shows a notification for both (added in the integration step). |
| 2026-09-21 | SI (Civil) goal | SLPRB AP SI (Civil), Post Code 11 is the THIRD goal (tag `SI`, exam name "SLPRB SI (Civil)"). Facts read from the official PDF (Rc.No.81/SLPRB/Rect.1/2026, 16-09-2026) and stored in `data/exam-specs/slprb_si_2026.json` (verified twice, by two agents). SI application dates are NOT in the PDF (press release). Plan and coverage matrix: `docs/si-civil-plan.md`. |
| 2026-09-21 | SI syllabus | ONE outline `slprb_si_written.json` serves Prelims Paper 1 + Final Paper III (Arithmetic and Reasoning) and Prelims Paper 2 + Final Paper IV (General Studies) because Annexure II gives one syllabus; English and Telugu (qualifying) are extra papers. Prelims marks do not count for final merit, so no separate Prelims outline. Urdu is not built (the owner takes Telugu). |
| 2026-09-21 | Group-I outlines | Old starter outlines (from coaching-site summaries) replaced by `appsc_g1_prelims_2026.json` / `appsc_g1_mains_2026.json`, built line by line from the Brief Notification (verified). Old files moved to `data/syllabus/_superseded/`; the seeder retires their PENDING copies through a `supersedes` field and never touches approved ones. The new outlines arrive as pending imports the owner approves (merge is on, so same-title topics are not doubled). |
| 2026-09-21 | Coverage proof | `data/exam-specs/coverage/*.json` maps every official syllabus line to an outline node; `backend/tests/test_syllabus_coverage.py` fails if a path stops resolving. Independent agents re-read both PDFs page by page: no omissions (SI 55 items, Group-I 233 + 445 items). |
| 2026-09-21 | Guide claims | The pasted "God Mode" guide is NOT evidence. Not in the Group-I brief notification: age limits, DSP height/chest/weight, past cut-offs. "No PET for DSP" is unverified and probably wrong: page 2 says physical requirements for post codes 03 and 04 are in the Detailed Notification (due by 06-10-2026). 167.6 cm / 86.3 cm are the SI notification numbers; SI women weight is 40 kg (not 45.5). |
| 2026-09-21 | Exam-name matching | Tag-to-exam matching is now whole-word (`features/examnames.py`), because the tag "SI" would match inside other names. Same fix applied in `revision/service.py` and Android `ReviseData.examProximity`. |
| 2026-09-21 | Physical training | While an SI exam is live the planner adds a 06:00 physical block (`phys-<date>`, ref `goals`), outside the study hours (skipped by hour-fitting, summary and weekly report). Session content is generic training advice, not from the notification. |
| 2026-09-21 | Aptitude drills | Arithmetic and reasoning practice is generated by the server with plain Python (`features/tests/aptitude.py`, 22 areas), exact answers, no AI, no negative marking (the SI PDF states none). One 20-question drill per day except Sunday (block `drill-<date>`, 19:30, ref `test/<id>`). Figure-based non-verbal reasoning cannot be generated as text: partial coverage, use imported past papers. |
| 2026-09-21 | Goals screen | Tablet route `goals` (not a rail item): dates and Applied ticks (`goal.applied`), SI profile (`si.profile`), checklist (`si.checklist`), PMT (`si.pmt`), PET log (`si.pet_log`), prelim cut-off calculator. Rules are hard-coded in `SiRules.kt` from the spec JSON; update both together when the Detailed Notification / SI press release arrives. Age window follows the PDF exactly (born 02-07-1999 to 01-07-2005; relaxations are not added together). |
| 2026-09-21 | Writing bank | 90 original practice prompts (`data/writing/si_and_group1_prompts.json`, English + Telugu) for the descriptive papers; a byte-identical copy ships as an Android asset (a test keeps them equal). Telugu text is AI-written and unchecked by a teacher. |
| 2026-09-22 | Brief date bug (fixed) | `POST /briefs/run` ("Prepare a brief now") wrongly dated new briefs at the NEXT occurrence of 00:00 India time (`next_occurrence("00:00", tz)`), i.e. almost always TOMORROW. A brief made in the morning showed dated tomorrow with no visible place in Today; a brief made the day before showed as today's once the date rolled over. Fixed to stamp `datetime.now(timezone.utc)` instead (`app/api/briefs.py`), with a regression test. The three already-mis-dated rows in the running laptop database (`backend-data/civils.db`) were corrected in place (scheduled_for set back to created_at); a backup `civils.db.bak-2026-09-22` was left next to it. Scheduled morning/evening briefs were never affected (they already used the correct time). |
| 2026-09-22 | Brief sync stale copy | Fixing `scheduled_for` directly in the database (2026-09-22 entry above) did not change `updated_at`, so the tablet's `GET /sync/pull` (which asks for rows with `updated_at >= cursor`) never saw the correction and kept showing the old wrong dates (Wed 23 Sep, Tue 22 Sep x2) offline. `updated_at` on those 3 rows was bumped to now; a normal sync now pulls the corrected dates. IMPORTANT: the server code fix (commit 20536c7) is not deployed until the laptop container is rebuilt - until then, new taps of "Prepare a brief now" will keep creating midnight-dated rows. |
| 2026-09-22 | Morning/evening briefs never fired | `settings.briefs` in the live database is correct (morning 07:00, evening 19:00, all days, enabled) and only the three manually-made "extra" briefs exist so far. Cause: the APScheduler cron job only fires while the laptop server process is actually running at that clock time (documented in docs/laptop-server.md: "the laptop must be on, awake and online at brief time"). This is expected for the current laptop-hosted setup, not a bug. |
| 2026-09-22 | Video channels | Curated a reference list of daily current-affairs YouTube channels (docs/video-channels.md), incl. Hareesh Academy (Telugu, explicitly tagged APPSC/TSPSC/Group-2/SI). Not auto-pulled: the app never downloads or transcribes video, per standing rules; the owner pastes a link into Videos manually. |
| 2026-09-22 | Mic "code 13" | Code 13 is Android's own `SpeechRecognizer.ERROR_LANGUAGE_UNAVAILABLE`: the tablet's offline speech engine does not have the requested language's pack installed (default listen language is en-IN; the tablet may only have en-US offline, or none). `VoiceInput.kt` now retries once online automatically when that happens (or `ERROR_LANGUAGE_NOT_SUPPORTED`, code 12), so it should just work when there is internet; only if the online retry also fails does it show a message naming the missing pack and the Settings path to add it. Not compiled locally (no Android SDK here); needs the next GitHub Actions build to confirm. |
| 2026-09-22 | UPSC syllabus (verified) | The owner supplied the real UPSC Examination Notice No. 05/2026-CSE (dated 04.02.2026, `UPSC-CSP-2026-Engl-060226Rev.pdf`, 159 pages). Read in full (Appendix I, pages 25-37) and used to replace the two starter outlines that were `verified: false`. New files `data/syllabus/upsc_cse_prelims_2026.json` (key `upsc-cse-prelims-2026`, 16 nodes/14 leaves: Prelims Paper I General Studies 7 items, Paper II CSAT 7 items) and `upsc_cse_mains_2026.json` (key `upsc-cse-mains-2026`, 80 nodes/72 leaves: Essay, GS I-IV, qualifying Paper A/Paper B, Interview) are built from the official bullet points verbatim, each with a `data/exam-specs/coverage/*-2026.json` proof file. Both `supersedes` their old unverified keys (`upsc-cse-prelims`, `upsc-cse-mains`); the old starter files moved to `data/syllabus/_superseded/`. The two Optional Subject papers (Paper VI/VII, 250 marks each, ~25 subject choices) are intentionally NOT built - each subject's syllabus runs 2-4 pages and the owner has not chosen an optional yet; add it as your own topics once you choose one. Essay Paper has no officially prescribed themes (only a general instruction), unlike the old starter which had invented essay-theme sub-topics - removed. |
| 2026-09-22 | Group-I cross-check against 5 more PDFs | The owner supplied 5 more Group-I-related PDFs. Findings: (1) `Group-I_Prilms_Syllabus_fnal_05.10.2018.pdf` (the official detailed Prelims syllabus) is word-for-word identical, including OCR typos ("Sathavahanas the Sangam Age", "Liberalisaion", "it's activities"), to what `appsc_g1_prelims_2026.json` was already built from - no changes needed, this independently confirms the prelims outline is complete. (2) `Group1pattern.pdf` (Notification 27/2018, pattern circular dated 19/03/2020) independently confirms the Mains paper structure already built: Paper II History/Culture/Geography, Paper III Polity/Constitution/Governance/Law/Ethics, Paper IV Economy & Development, Paper V Science & Technology, each 150 marks/180 minutes, plus separate Telugu and English qualifying papers - matches `appsc_g1_mains_2026.json`. (3) `Group-I_Main_Syllabus_final_05.10.2018.pdf`'s Telugu paper (13 items incl. "Debate Writing" 10 marks and "Dialogue Writing" 10 marks, no Word Meanings/Synonyms-Antonyms items) DIFFERS from the current app's Telugu paper (13 items incl. Word Meanings, Synonyms/Antonyms, Translation both ways, no Debate/Dialogue Writing). Checked byte-for-byte against `Group_I_072026.pdf` page 13 (the actual 2026 Brief Notification the app was built from): the app's version matches the 2026 notification exactly, word for word. **Resolved: the Telugu paper was revised between the 2018 detailed syllabus and the 2026 notification; the 2026 notification governs this recruitment cycle, so the app is already correct - no change made.** (4) `Scheme_Syllabus-2011-Group-I-New.pdf` (Notification 18/2011) is the OLD 2011 scheme: different Screening Test format, a Paper V "Data Interpretation and Problem Solving" instead of the current Science & Technology paper, and History+Constitution combined into one paper instead of split. Confirmed superseded by the 2018/2020/2026 restructuring; not used. (5) `Group1_APSSC.pdf` is a reference-books reading list (no syllabus content); not actionable for the outline, kept only as owner reading material context. |
| 2026-09-22 | SI (Civil) cross-check | `AP-Police-SI-Exam-Topics.pdf` is RC.No.216/R&T/Rect.1/2018 - an OLDER, DIFFERENT AP Police SI recruitment (not the SLPRB 2026 one, Rc.No.81/SLPRB/Rect.1/2026, that `slprb_si_2026.json` is built from). Its Prelim Paper I (Arithmetic SSC standard + Reasoning, 100Q) and Paper II (General Studies, 100Q, same 5 topics: General Science, current events, History of India, Geography of India, Indian Polity and Economy) structurally match what is already verified for 2026 - a useful independent confirmation that the format is stable across cycles, but it is not itself a source for the current cycle's facts; no change made to `slprb_si_2026.json` or `slprb_si_written.json`. |
