package com.naveen.civilscompanion.ui.voice

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.navigation.NavHostController

/**
 * OWNER: Ask / voice (M6). The always-available microphone button. AppRoot places it bottom-right over every screen.
 * Draw nothing until there is something to show (the owner asked for it to be small and out of the way).
 */
@Suppress("UNUSED_PARAMETER")
@Composable
fun FloatingMic(nav: NavHostController, modifier: Modifier = Modifier) {
    // built in M6
}
